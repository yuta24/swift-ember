// A SwiftUI application needs one product and one import. SpliceSwiftUI's
// target dependency brings the loader with it; the exported import also makes
// `Splice.start()` available at the call site.
@_exported import SpliceRuntime
