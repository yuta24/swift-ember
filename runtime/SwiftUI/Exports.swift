// A SwiftUI application needs one product and one import. EmberSwiftUI's
// target dependency brings the loader with it; the exported import also makes
// `Ember.start()` available at the call site.
@_exported import EmberRuntime
