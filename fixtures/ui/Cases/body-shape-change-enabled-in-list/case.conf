EXPECT=alive
EXPECT_REGISTERED=2
EXPECT_RENDERED_PREFIX="new|"
PRESERVE_RENDERED_SUFFIX=yes
NOTE="AnyView at an explicit opt-in boundary makes a List row shape change safe, observable, and state-preserving"
