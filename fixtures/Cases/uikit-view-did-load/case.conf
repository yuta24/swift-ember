PLATFORMS="simulator"
STATE_PRESERVED=yes
NOTE="a replaced viewDidLoad does not re-run by itself; discarding the view re-runs it with the controller's state intact, and the rebuilt view is measured as not reinstalled"
